<h1><%= @club.name  %></h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

<% @courses.each do |course| %>
  <tr>
    <td><%= course.name %></td>
    <td><%= link_to 'Show', club_course_path(@club.id, course.id) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Course', new_club_course_path %>
